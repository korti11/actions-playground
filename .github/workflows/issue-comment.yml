name: Issue Listener

on:
  issue_comment:
    types: [edited]

jobs:
  event-print:
    name: "Event print"
    runs-on: ubuntu-latest
    steps:
      - run: echo "$CONTEXT"
        env:
          CONTEXT: ${{toJson(github.event)}}